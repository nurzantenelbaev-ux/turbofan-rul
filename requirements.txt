# Core scientific stack
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
joblib>=1.3
pyarrow>=14.0          # parquet I/O for processed feature files

# Models
xgboost>=2.0
torch>=2.1             # any 2.x release works on CPU; install the CUDA build
                       # if you have an NVIDIA GPU (see README installation notes)

# Tests / formatting (optional — used by the CI block in the Makefile)
pytest>=7.4
ruff>=0.1
